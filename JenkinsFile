pipeline {
    // ใส่ชื่อ Agent ตัวเอง
    agent { label 'kanchala-agent' }
    stages {
        stage('My simple stage') {
            steps {
                sh "/Users/miwdoppio/training/send_msg.sh ThisIsMyJenkinsFile-Oz"
            }
        }
    }
}